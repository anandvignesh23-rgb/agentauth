services:
  agentauth:
    image: node:26-alpine
    working_dir: /app
    command: sh -c "npm run seed && npm start"
    ports:
      - "8787:8787"
    volumes:
      - .:/app
    environment:
      AGENTAUTH_TOKEN_SECRET: dev-only-token-secret
